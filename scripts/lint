#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
corepack pnpm run lint
for script in scripts/*; do
  if [[ "$(head -n 1 "$script")" == '#!/usr/bin/env bash' ]]; then
    bash -n "$script"
  fi
done
