#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
scripts/check-generated
scripts/format-check
scripts/lint
scripts/typecheck
scripts/test-unit
uv lock --check --offline
scripts/build
scripts/test-integration
echo 'Workspace, API, and client validation passed (generated drift, format, lint, typecheck, unit tests, lock, builds).'
echo 'Local PostgreSQL and Floci integration passed.'
echo 'Phase 1 remains incomplete: no client applications, provider mocks, released-contract compatibility, security scan, or CDK synth yet.'
