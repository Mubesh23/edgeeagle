[project]
name = "edgeeagle-domain"
version = "0.0.0"
description = "Provider-neutral EdgeEagle domain primitives"
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/edgeeagle_domain"]
