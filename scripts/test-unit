#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
uv run --locked --offline --all-packages pytest "$@"
node --test scripts/tests/*.test.mjs
PATH="$(pwd)/node_modules/.bin:$PATH" TURBO_TELEMETRY_DISABLED=1 node_modules/.bin/turbo run test
